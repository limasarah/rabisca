name: rabisca
description: Aplicativo infantil de desenho colaborativo seguro.
publish_to: 'none'
version: 0.1.0+1
environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  provider: ^6.0.5
  socket_io_client: ^2.0.0

flutter:
  uses-material-design: true
